- 阶段1实现：
  - 存储升级：新增 mood_records_v2 结构（{ mood, note?, ts }），并在 app 启动迁移 v1→v2（幂等回退）。
  - 日历增强：回页自动刷新（onShow）、按心情着色（utils/colors.js）、跨月点击联动、长按进入详情。
  - 详情页：pages/detail 支持选择心情与备注编辑、保存与清除。
  - 配置更新：app.json 注册 pages/detail/index。
  - 兼容接口：storage.getMap() 仍返回日期→emoji 映射，兼容日历页使用。
